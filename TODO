Update the CMakeLists.txt so that it uses many CMakeLists.txt in included directories (I need to learn more CMake first;
this does the job for now).

Rewrite deck to directly store Card objects, not pointers to card objects (no double referencing which is slow).
Replace bet_history in player with a dynamic array.

OR create a 2D obj class which replaces gameobj and is a struct member of widget; the anim uses this as target.
Create a union of int/float/bool wrapper structs which is passed as cargo with the animation.

in render_label, create a retex flag in label struct which controls whether the label texture is re-rendered.
Widgets to store texture_map/font_map keys or references.

Create inputlistener event eventlistener structs which are members of structs that do them.
Create vtables for input and event listening.
Use wrapper structs like for the type when registering the struct to event/input listening. This wrapper
struct is not dynamically allocated.

How am I going to pass other variables to a widget when calling its update method?
One option is to create an 'observables' dynamic arr which holds pointers to observed values. Initialise this during
widget creation.
Other option is to change definiton of the update_func to allow vararg cargo variables to be passed.
Similar to how Animation is set up to hold cargo as well. Also allow Anim to carry a variable amount of cargo (this will
need dynamic allocation).