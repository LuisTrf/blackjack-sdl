
BUGS
Pressed mouse going into button box does not trigger button to be pressed (this is from SDLMOUSEBUTTONDOWN only triggering when lmb is pressed not held).
label resizing bug when NOT updating player_money_label in update()
Labels which do not share font size should use different fonts; this will prevent jittering when updating label.

TODO
Ensure sdl_ttf only renders new textures when text is updated.
Ensure coordinates and state is only modified in update not in render/animate (there is a bit now but don't be sloppy with this).


Add completed flag to Animation struct.
Rename CHIP_VALUES enum to CHIP_VALUE
Separate game logic from the animation methods (would need to create event manager for this).
Remove modifying stack_button spritesheet outside of render.c
Go through the whole codebase and refactor/rename methods/variables to keep consistency.

docstrings using doxygen
update button_events.c to use self
Disable valued buttons when stack is full.
Dealer needs to stand on soft 17.
Work on resource organisation and properly do the chip spritesheets to add hover/disabled feedback (light blue/gray?) (excluding black which is good).
Dark bg chips use white hovered border, white bg chips use black hovered border

#DEFINE for payout odds.
move label code to button_events instead of update so they run only when labels get updated
move update_label_visibility to button_events somewhere

betting with $0 should reset game state to where as if bet button hadn't been pressed
money label should wait until after animation before updating itself on a blackjack
remove 'magic numbers' from .c to .h files
Label code is all over the place fix it up.

implement the observer design pattern for labels