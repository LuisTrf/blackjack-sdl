Consider changing to arrays of structs instead of arrays of struct pointers for more efficient memory access.

Widget subscribe to another widget.
When the publishers updates, it calls an notify_all(event) method on the subscribers.
Pack struct members more efficiently to minimise padding.
Create spritebox struct.

Textures should get loaded into a hashmap.