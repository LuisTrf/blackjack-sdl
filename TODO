Update the CMakeLists.txt so that it uses many CMakeLists.txt in included directories (I need to learn more CMake first;
this does the job for now).

Remove constructors which do not dynamically alloc memory with type casting.

ui notify methods should be in separate source file.

go through game.h/game.c again to rename appropriately and optimise.
types.h?
build objects directly on heap dont build on stack then deference.

Replace calloc with [] for fixed arrays (e.g. deck, queues).

clean up the event queue, create functions and poll event method.
proper 0 check func for floats

bet button not visible/idle on player blackjack
cache game_ctx-> vars

reduce code duplication

decompose header files

proper money formatting 100,000

decouple widget dimensions from spritesheet dimensions
event .data variables should be the name of the event to which the data belongs

re-implement 5-card charlie
put newlines in text to cause wrapping when doing splitting
make event prefabs in header files

game_state_playing_split put cards into split hand. start by putting game_state to game_state_playing_split, when
bust or stand put into game_state_playing and continue as normal.
>1 game state where it is different when betting